%article.video_category
  %header
    %h3 TV Commedies
    %hr
  .videos.row
    - @category.videos.each do |video|
      .video.col-sm-2
        = link_to video_path(video), nil do
          %img(src="/tmp/#{video.small_cover_url}.jpg")
